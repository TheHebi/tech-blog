<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="posted">
            {{user.username}} on {{dateFormat created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>